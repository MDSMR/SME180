<?php
// TEMP DEBUG MESSAGE — REMOVE AFTER TESTING
if (isset($_GET['debug']) && $_GET['debug'] === '1') {
    echo '<div style="position:sticky;top:0;z-index:9999;background:#fffbcc;
        border:2px solid #ffdd55;
        padding:10px 15px;
        font:bold 14px/1.4 system-ui;
        color:#444;">
        ✅ This is the LIVE items.php file — '.date('Y-m-d H:i:s').'
    </div>';
}
?>